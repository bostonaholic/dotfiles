#!/bin/bash
################################################################################
# Pairing - Remote Pair Programming Setup
#
# DESCRIPTION:
#   Establishes SSH reverse tunnel for remote pair programming.
#   Allows remote user to connect back to your local machine through
#   a public server.
#
# USAGE:
#   pairing
#
# WHAT IT DOES:
#   Creates SSH reverse tunnel:
#   - Local port 22 (SSH) -> Remote port 1337 on pairing server
#   - Pair partner can connect: ssh -p 1337 localhost (from pairing server)
#
# DEPENDENCIES:
#   - SSH access to pairwith.matthewboston.com
#   - SSH server running locally (port 22)
#
# SECURITY NOTES:
#   - Exposes local SSH to pairing server
#   - Only use with trusted pairing servers and partners
#   - Tunnel closes when script exits
#
# NOTES:
#   Personal pairing server configuration.
#   Modify hostname/user for your own pairing setup.
#
################################################################################

set -eux -o pipefail

ssh -R1337:127.0.0.1:22 matthew@pairwith.matthewboston.com
