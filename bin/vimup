#!/bin/bash

set -euo pipefail

(
    cd "${HOME}/.vim_runtime"
    git reset --hard
    git clean -d --force
    git pull --rebase
    python update_plugins.py
)

echo
echo "# TODO: If exit 1 try again after installing 'requests' module"
echo "# pip3 install requests"
